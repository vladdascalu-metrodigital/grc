import React from 'react';
import { storiesOf } from '@storybook/react';
import BackButton from '../BackButton';

storiesOf('Fundamentals/BackButton', module).add('Simple Back Button', () => <BackButton />);
