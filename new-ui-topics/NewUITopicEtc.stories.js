import React, { useState } from 'react';
import { storiesOf } from '@storybook/react';

storiesOf('New UI Topics', module).add('AppShell, PageHeader, MainMenu, PageTitle, Language List', () => {
    return <div>See App Structure Section</div>;
});
